# ARIA v2.0 環境變數設定範例

複製此檔案為 `.env` 並填入實際的設定值。

```bash
# ARIA v2.0 設定閾值
SLOPE_THRESHOLD=30      # 高風險坡度閾值（度）
ELEVATION_LOW=50        # 低高程閾值（公尺）
BUFFER_HIGH=500         # 避難所緩衝區半徑（公尺）
TARGET_COUNTY=花蓮縣    # 分析目標縣市
```

## 使用說明

1. 複製此檔案：
   ```bash
   cp .env.example .env
   ```

2. 根據你的需求修改 `.env` 檔案中的數值

3. `.env` 檔案已加入 `.gitignore`，不會上傳到 GitHub

## 注意事項

- 請勿將 `.env` 檔案提交到版本控制系統
- 如需分享設定，請使用 `.env.example` 檔案
- 敏感資訊（如 API 金鑰）應放在 `.env` 中，而非程式碼內
